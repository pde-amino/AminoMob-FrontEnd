import { View, Text } from "react-native";
import React from "react";

const NavbarComponents = () => {
  return (
    <View
      style={{
        height: 50,
        backgroundColor: "red",
      }}>
      <Text>NavbarComponents</Text>
    </View>
  );
};

export default NavbarComponents;
